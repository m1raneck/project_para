#include <stdio.h>
#include <math.h>

int pow(int a, int b) { return pow(a, b); }

int main( void )
{
    printf("hello world\n");
    printf("%d", pow(2, 3));

    for (int i = 0; i < 5; ++i)
        printf("hello git\n");
        
    return 0;
}
